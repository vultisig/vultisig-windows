{
  "name": "vultisig-win",
  "version": "0.1.0",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^9.8.0",
    "husky": "^9.1.4",
    "lint-staged": "^15.2.8",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "frontend/src/**/*.{js,jsx,ts,tsx}": [
      "cd frontend && eslint --fix",
      "cd frontend && prettier --write"
    ]
  }
}
