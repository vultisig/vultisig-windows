import styled from 'styled-components'

import { vStack } from '../../lib/ui/layout/Stack'

export const PeersManagerFrame = styled.div`
  ${vStack({
    fullWidth: true,
    gap: 24,
    alignItems: 'center',
  })}
`
