import { verticalPadding } from '@lib/ui/css/verticalPadding'
import styled from 'styled-components'

import { ModalContainer } from '../../../modal/ModalContainer'

export const QueryOverlayContent = styled(ModalContainer)`
  ${verticalPadding(20)}
`
