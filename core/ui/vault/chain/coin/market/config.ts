/**
 * Rendered height of the price chart's plot area, shared by the chart and
 * its loading placeholder so the card never shifts when the series arrives.
 */
export const chartHeight = 168

/**
 * Normalized SVG coordinate space the series paths are computed in; the
 * svg stretches it to the container via `preserveAspectRatio="none"`.
 */
export const chartViewBoxSize = 100
