/**
 * Rendered height of the limit chart's plot area, shared by the chart and its
 * loading placeholder so expanding the card never shifts the form under it.
 */
export const limitChartHeight = 148

/**
 * Normalized SVG coordinate space the series path is computed in; the svg
 * stretches it to the container via `preserveAspectRatio="none"`.
 */
export const limitChartViewBoxSize = 100
