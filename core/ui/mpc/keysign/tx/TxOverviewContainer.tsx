import { PageContent } from '@lib/ui/page/PageContent'
import styled from 'styled-components'

export const TxOverviewContainer = styled(PageContent)`
  width: min(100%, 550px);
  margin-inline: auto;
  padding-top: 64px;
`
